{% extends 'admin/index.html.twig' %}

{% block body %}
{{form_start(form)}}
<div class="mb-3">
  <label  class="form-label">Category Name</label>
  {{form_widget(form.name)}}
</div>

 <button type="submit" name="admin_create_category" class="btn btn-success m-5">Confirm</button>
{{form_end(form)}}

{% endblock %}